jenkins_inventory_data: ../../inventory_files/jenkins_inventory_compose.jenkins.yml

computers_json: all_computers.json

nodes_xml:
    - ../../nodes/node1.xml

expect:
    groups:
        all:
            - node1

        ungrouped: []

        temporary_offline:
            - node1

        my_label_one:
            - node1

        oss_ssh_slaves:
            - node1
